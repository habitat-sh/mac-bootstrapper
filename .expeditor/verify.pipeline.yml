---
expeditor:
  defaults:
    buildkite:
      timeout_in_minutes: 30

steps:

- label: ":bash: Shellcheck"
  commands:
    - shellcheck $(find . -name \*.sh)
  expeditor:
    executor:
      docker:
